{
  "name": "wtvmea-checker",
  "version": "1.0.0",
  "description": "Check if WTVMEA results are out, once every 30 seconds",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm install && node index.js"
  },
  "author": "Micah Lindley",
  "license": "MIT",
  "dependencies": {
    "puppeteer": "^10.4.0",
    "express": "^4.17.1",
  }
}
